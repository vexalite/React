import React from "react";

export default function Flipkart(){
  return (
    <div>
      <p>Whether you're using React or another library, Create React App lets you focus on code, not build tools. To create a project called my-app, run this command:.
      </p>
    </div>
  );
}