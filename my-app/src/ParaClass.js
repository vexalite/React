import React from "react";

export default class FlipkartClass extends React.Component{
    render(){
        return (
            <div>
                <p>The downside of step 1 can be alleviated (based on your situation) by creating an array of images that can be imported into a component. Import into the component that needs it. Even better is create a directory with all your images or icons and an index.js that exports that array. The actual image in the array or image object is added with require('./image-url.svg')</p>
            </div>
        )
    }
}